name: Build unsigned GainTrack IPA

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'FitnessTracker/**'
      - '.github/workflows/build-gaintrack-ipa.yml'

jobs:
  build:
    runs-on: macos-15
    defaults:
      run:
        working-directory: FitnessTracker

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select latest available Xcode
        run: sudo xcode-select -switch /Applications/Xcode.app

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build unsigned .app
        run: |
          xcodebuild \
            -project GainTrack.xcodeproj \
            -scheme GainTrack \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM="" \
            clean build \
            -derivedDataPath build

      - name: Package as unsigned IPA
        run: |
          mkdir -p Payload
          cp -R "build/Build/Products/Release-iphoneos/GainTrack.app" Payload/
          zip -r GainTrack-unsigned.ipa Payload

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: GainTrack-unsigned-ipa
          path: FitnessTracker/GainTrack-unsigned.ipa
